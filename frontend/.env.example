# Local development can leave this blank when using a dev proxy or same-origin API routing.
# Production should set this to the deployed backend URL, for example:
# VITE_API_BASE_URL=https://your-backend.example.com

VITE_API_BASE_URL=
